# Quest Timer

## 릴리스 자산 보관 규칙

GitHub 릴리스에 올린 파일(자산)은 아래 기준으로 남기고 지운다.
**릴리스와 태그 자체는 지우지 않는다. 자산만 지운다.**

- **최신 릴리스** — 자산을 **전부 남긴다**. `latest.yml` 과 `.blockmap` 은
  자동 업데이트가 읽는 파일이라 하나라도 빠지면 업데이트가 멈춘다.

- **마디가 되는 판 (x.y.0)** — 1.0.0, 1.1.0, 1.2.0 처럼 끝자리가 0인 버전은
  최신 자리에서 내려온 뒤에도 **exe 만 영구히 남긴다**. `latest.yml`,
  `.blockmap` 같은 곁다리 파일은 지운다. 나중에 그 시점의 앱을 받아볼 수
  있으면 된다.

- **소소한 업데이트 (x.y.z, z ≥ 1)** — 1.2.1, 1.2.2, 1.2.3 같은 잔가지 판은
  최신 자리에서 내려오는 즉시 **자산을 전부 지운다**. 곧, 잔가지 판은 언제나
  가장 최신 하나만 파일을 들고 있다.

새 판을 올린 뒤에 바로 앞 판을 정리한다. 1.2.5 를 올렸으면 1.2.4 의 자산을 전부
지운다. 1.3.0 을 올렸으면 1.2.4 를 비우되 1.2.0 의 exe 는 그대로 둔다 - 마디 판은
최신에서 내려와도 남는다.

지울 때는 자산 id 로 지운다. 같은 태그에 릴리스가 둘일 수 있어 태그 이름만으로는
어느 쪽인지 가려지지 않는다.

```
gh api repos/glglekdy/quest-timer/releases --jq '.[] | "\(.tag_name) \([.assets[] | "\(.id) \(.name)"] | join(" | "))"'
gh api -X DELETE repos/glglekdy/quest-timer/releases/assets/<asset id>
```

옛 자산을 지워도 자동 업데이트에는 지장이 없다. 최신 릴리스 하나만 읽고,
차등 다운로드가 쓰는 blockmap 도 설치된 쪽에 캐시된 것을 쓴다.
